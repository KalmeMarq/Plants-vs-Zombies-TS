import App from "..";
import GuiScreen from "./GuiScreen";

export default class OptionsDialog extends GuiScreen {
  public constructor(app: App) {
    super(app)
  }

  public init(): void {
  }

  public update(): void {
  }
}